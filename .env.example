# 配信停止URLのドメイン（本番では公開URLを設定）
# 例: https://mailwise.example.vercel.app
APP_BASE_URL=http://localhost:3000

# 本番(Vercel)用: Turso(libSQL) の接続情報。
# 未設定の場合はローカルの data/mailwise.db（SQLiteファイル）を使用します。
# TURSO_DATABASE_URL=libsql://your-db-xxxx.turso.io
# TURSO_AUTH_TOKEN=your-token
